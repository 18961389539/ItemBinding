# ItemBinding 软件 UI 在 1024×768 分辨率下的适配问题审查

- 审查日期：2026-09-15
- 审查范围：`MainAPP` 全部 XAML 视图 + 窗口尺寸相关 C# 逻辑（33 个 XAML 文件）
- 审查方式：静态布局测算（固定尺寸 / 最小尺寸 / 网格比例 / 滚动策略），未做目标机实测
- 参照环境：当前开发机 1600×900 @100% 缩放；目标 1024×768（工业 PC）

---

## 一、可用空间测算（1024×768 @100% 缩放）

| 项目 | 数值 | 说明 |
|---|---|---|
| 屏幕 | 1024 × 768 | |
| 任务栏 | −40 | 按底部任务栏估算 |
| **工作区** | **1024 × 728** | 窗口可用的最大区域 |
| 最大化后客户区 | ≈ 1024 × 728 | WPF 最大化时边框被顶出屏幕，客户区等于工作区 |
| 主窗口固定占用 | −88 | 顶部状态栏 60 + 系统状态条 28 |
| 主内容区 | ≈ 1024 × 640 | |
| 左侧竖排导航 Tab 条 | −70 | 7 个 Tab，含图标 + 文字 + Padding 20,15 |
| 内容 Border Margin | −20 | `Margin="10"` 上下左右 |
| **各视图实际可用区域** | **≈ 934 × 620** | 对比设计基准（HomeView `d:DesignWidth=1200`）**少约 22% 宽、17% 高** |

**核心矛盾**：整套 UI 的尺寸基准是 1400×1200 级别，而 1024×768 下只有约 934×620 的可用空间。

---

## 二、问题清单

### P0 — 阻断级（窗口本身装不下屏幕）

#### P0-1　主窗口默认尺寸超出屏幕，且无任何屏幕钳制

**证据**

| 位置 | 内容 |
|---|---|
| `MainWindow.xaml:14-17` | `Width="1400" Height="900" MinWidth="1024" MinHeight="700"` |
| `Models/Settings/UiSettings.cs:11,16` | 默认 `WindowWidth=1200`、`WindowHeight=800` |
| `MainWindow.xaml.cs:128-129` | `Width = Math.Max(1024, settings.WindowWidth);`<br>`Height = Math.Max(700, settings.WindowHeight);` |
| `Services/NavigationService.cs:209-210` | 同一逻辑重复一遍，同样硬编码 1024 / 700 |
| 全项目 | **无** `SystemParameters.WorkArea` / `PrimaryScreenWidth` 等屏幕尺寸判断 |

**后果**

- 首次运行取设置默认值 → 窗口 **1200×800**；而工作区只有 **1024×728**。
- 窗口比屏幕**宽 176px、高 72px**，因此**必然**有 176×72px 的内容落在屏幕外——落在哪一侧取决于系统摆放：
  - `WindowStartupLocation="CenterScreen"` 居中的话，左右各溢出 **88px**：左侧的竖直导航 Tab 条（约 70px 宽）整体被推出可视区 → **用户点不到任何功能页**；顶部溢出 **36px**，标题栏被切。
  - 即便系统把窗口挪正（左对齐），右侧 176px 仍一定在屏幕外 → 顶栏右端的「清零 / 登录」按钮不可达。
- 窗口宽度下限被代码硬钉在 `Math.Max(1024, ...)`，**用户无法把窗口缩小到屏幕以内**（1024 恰好等于屏幕宽度，零余量，连边框都不够放）。
- 注：改动设置里的窗口尺寸也没用——保存值小于 1024 会被 `Math.Max` 顶回 1024。

---

#### P0-2　ChartsView（图表分析）最小尺寸大于屏幕

**证据**：`Views/ChartsView.xaml:12-15`

```
Width="1600" Height="1000" MinWidth="1200" MinHeight="800"
```

由 `Services/DialogService.cs:34`（`new ChartsView(items)`）打开，入口在数据页/主页的"查看图表"。

**后果**

- `MinWidth=1200` > 屏幕宽 1024、`MinHeight=800` > 工作区高 728 → **用户无论怎么拖拽都无法把窗口放进屏幕**。
- 顶部 6 个标签（Trend / Distribution / Barcode / Scatter / 质量复盘 / SPC 控制，`ChartsView.xaml:291/361/431/501/555/671`，字号 14）合计约 790px，在 1200 宽下正常；但窗口被钉在 1200 且居中时左右各溢出 88px，**首尾标签会被裁掉**。
- 13 个图表每个固定 `Height=300~460`，虽然包在 `ScrollViewer` 里可纵向滚动，但横向可用宽度被压缩后图表会变窄、刻度拥挤。

---

#### P0-3　RecipeWindow（配方详情）最小宽度等于屏幕宽度

**证据**：`Views/RecipeWindow.xaml:16-19`

```
Width="1200" Height="800" MinWidth="1024" MinHeight="680"
```

**后果**

- 宽 1200 > 1024 → 同 P0-1，至少 176px 在屏幕外。
- `MinWidth=1024` 恰好等于屏幕宽度 → **零收缩余量**；`MinHeight=680` + 标题栏 31 = 711 < 728 勉强能站住，但配合 800 的默认高度就超出工作区 72px，**底部按钮区不可达**。
- 配方详情是高频操作页（切换产品/改参数），底部通常是保存/取消按钮，不可达即功能不可用。

---

### P1 — 明显影响操作（可用但很难用）

#### P1-1　HomeView 右侧面板被压缩到约 360px，而表格有 9 列

**证据**：`Views/HomeView.xaml:36-38` 采用 `3* / Auto / 2*` 三列（中间 3px 分隔条）

| 环境 | 内容宽 | 图像区(3/5) | 右侧面板(2/5) |
|---|---|---|---|
| 1600×900（当前开发机） | ≈1250 | ≈750 | ≈500 |
| **1024×768** | **≈934** | **≈557** | **≈373** |

右侧面板 Tab1「推理结果」DataGrid（`HomeView.xaml:170-187`）有 9 列：置信度 / 中心X / 中心Y / 角度 / 掩码面积 / 亮灰度 / 暗灰度 / 亮差 / 条码(*)。
前 8 列 `Width` 未显式设置 → WPF 默认 `Auto`，中文表头实测每列约 70-90px，**合计约 620-720px**。在 373px 的面板里只能看到约 4 列，其余全靠横向滚动条翻。

Tab2「发送」（`HomeView.xaml:249-274`）固定列 `90+50+70+70+70=350`，加上 条码(*) → 在 373px 里条码列被压到接近 0 宽。

#### P1-2　主窗口顶栏（60px）横向拥挤，用户状态可能被裁

**证据**：`MainWindow.xaml:52-205`

- 顶栏是 `*` + `Auto` 两列；左列内是**横向 `StackPanel`**（组1 计数 / 组2 设备 / 组3 用户），其中含 `MinWidth="56"`×3、`MinWidth="80"`、多个 16/20px 外边距和 12px 分隔线间隔。
- `StackPanel` **不会收缩子元素**，宽度不足时直接溢出裁剪。
- 右列（`Grid.Column="1"`）是「清零 / 登录」按钮，右对齐。

**后果**：宽度不够时，左列末尾的「组3：用户登录状态」（角色图标 + 登录状态 + 会话剩余时间）先被裁掉，只剩右侧按钮。同时该区域与 P0-1 的屏幕溢出叠加。

#### P1-3　LogView 详情抽屉固定 300px，一出现就挤垮表格

**证据**

| 位置 | 内容 |
|---|---|
| `Views/LogView.xaml:885` | 右侧详情抽屉 `Width="300"` |
| `Views/LogView.xaml:802` | `DataGridTemplateColumn Width="300" Header="异常"` |
| `Views/LogView.xaml:128/141/154` | 列 `MinWidth="92" / 92 / 170` |

**后果**：934px 内容宽下，选中任意一条日志 → 抽屉出现（`Visibility` 由触发器切换）→ **表格可用宽度从 ~930 骤降到 ~620**，而单「异常」列就固定占 300px，叠加其余列的 MinWidth 后必然横向滚动。抽屉的显隐还会造成整页 300px 的**布局跳动**。

#### P1-4　SettingsView「通讯协议」页固定 340px 左栏

**证据**：`Views/SettingsView.xaml:726` `ColumnDefinition Width="340"`（左栏协议列表，含 739 行 `DataGrid Height="300"`）；`:863` `Width="150"`

**后果**：934px 下右栏仅剩约 568px，且 340px 不参与收缩。不致命，但窄屏下右侧表单会比较局促。其余 5 个设置页（界面/数据保留/通信/检测与去重/安全）用的是 `Auto`+`*`，属正常。

---

### P2 — 体验与边缘问题

#### P2-1　无 DPI 适配声明（1024×768 场景下最容易被忽略的致命项）

**证据**：`MainAPP.csproj` 仅有 `<UseWPF>true</UseWPF>`，**无** `ApplicationHighDpiMode` / `ApplicationManifest`；全仓库无自定义 `.manifest`。

**后果**：1024×768 的工业屏若 Windows 缩放不是 100%（现场常见 125%/150%），WPF 的逻辑坐标（DIP）空间会被缩小：

| 缩放 | 有效 DIP 空间 | 能否容纳 MinWidth=1024 的窗口 |
|---|---|---|
| 100% | 1024 × 768 | 勉强（零余量） |
| 125% | 819 × 614 | **不能** |
| 150% | 683 × 512 | **不能** |

**建议**：显式声明 DPI 感知模式并在目标机统一 100% 缩放，否则 P0 问题会被进一步放大。

#### P2-2　左侧导航 Tab 条在窗口最小高度下几乎无余量

7 个 Tab × （图标 24 + 间距 4 + 文字约 15 + `Padding 20,15` 的 30 + `Margin 0,2` 的 4）≈ **539px**。
`MinHeight=700` 时内容区约 581px → **仅余约 42px**。字体渲染差异或 DPI 变化即可让最后一个「关于」页被裁掉（WPF `TabControl` 在 `TabStripPlacement=Left` 下**没有溢出箭头**）。

#### P2-3　LoginWindow 只适合 100% 缩放

**证据**：`Views/LoginWindow.xaml:7-8` `Width="920" Height="600"`，左栏 `1.1*` 内含 `240×240` 与 `160×160` 图形。
100% 下可放；125% 缩放（有效宽 819 DIP）即放不下。

#### P2-4　ChartsView 标签条宽度接近上限

6 个英文标签在字号 14 下合计约 790px，1024 下可用约 966px，横向**勉强够用**，但一旦窗口被 MinWidth 钉在 1200 并居中，首尾标签仍会落在屏幕外（见 P0-2）。

#### P2-5　通知浮层固定 320px —— 无风险

`MainWindow.xaml:567-579` 的 Growl 通知容器 `Width="320"`、居中、`MaxHeight=600`，1024 下正常。

#### P2-6　`d:DesignWidth/Height` 不是问题（避免误判）

`HomeView(1200)`、`LogView(1200)`、`AboutView(720)`、`DatabaseView(1000)` 的 `d:Design*` **仅在 VS 设计器生效，运行时不参与布局**，不要当缺陷改。

#### P2-7　做得对的地方（无需改动）

- `DatabaseView.xaml:104` 搜索工具栏用 `WrapPanel` → 窄屏自动换行，是正确做法。
- `SettingsView` 每个 Tab 内部都有 `ScrollViewer VerticalScrollBarVisibility="Auto"` → 纵向溢出可控。
- `AboutView.xaml:30` 同样有外层 `ScrollViewer`。
- `RecipesManageView` 的 300px 左栏配了 `MinWidth="250"`，且有分隔条。

---

## 三、修复建议（按性价比排序）

### 1. 启动时按工作区钳制窗口尺寸（最高性价比，一处改动解决 P0-1/P0-3）

`MainWindow.xaml.cs:123-131` 与 `Services/NavigationService.cs:200-213`：

```csharp
private void ApplyWindowSettings()
{
    var settings = Settings.Instance;
    var wa = SystemParameters.WorkArea;          // 当前显示器工作区（已排除任务栏）
    // 目标尺寸夹在 [安全下限, 工作区] 之间，避免窗口大于屏幕
    Width  = Math.Min(Math.Max(900, settings.WindowWidth),  wa.Width);
    Height = Math.Min(Math.Max(600, settings.WindowHeight), wa.Height);
    Title  = settings.WindowTitle;
}
```

同时把 `Math.Max(1024, ...)` / `Math.Max(700, ...)` 这两处**硬编码**替换为常量或配置项（注意 `NavigationService` 里还有一份重复实现，建议抽成一个方法避免再次分叉）。

### 2. 下调各窗口的最小尺寸

| 文件 | 现状 | 建议 |
|---|---|---|
| `MainWindow.xaml:16-17` | `MinWidth=1024` `MinHeight=700` | `900 / 600`（或按屏幕比例动态设置） |
| `Views/ChartsView.xaml:14-15` | `MinWidth=1200` `MinHeight=800` | `900 / 600` |
| `Views/RecipeWindow.xaml:16-19` | `MinWidth=1024` `MinHeight=680` | `900 / 600`，默认尺寸按工作区 90% 计算 |

### 3. 独立窗口默认尺寸改为自适应

`ChartsView` / `RecipeWindow` 的 1600×1000 / 1200×800 改为：

```csharp
Width  = Math.Min(1600, SystemParameters.WorkArea.Width  * 0.95);
Height = Math.Min(1000, SystemParameters.WorkArea.Height * 0.95);
```

### 4. HomeView 窄屏适配（对应 P1-1）

- 给图像与右侧面板的 `ColumnDefinition` 设 `MinWidth`，避免右栏被压到 360px 以下（例如右栏 `MinWidth=420`，不足时整体横向滚动）。
- 或窄屏（`ActualWidth < 1100`）时把 `3*:2*` 切换为 `1*:1*`，并给推理结果 DataGrid 的 8 个 `Auto` 列显式设 `Width`，让横向滚动条行为可预期。

### 5. LogView 抽屉改为覆盖式浮层（对应 P1-3）

窄屏时把 300px 抽屉从**占位布局**改为覆盖在表格之上的浮层（`Panel.ZIndex` + 半透明背景），避免表格宽度骤降与布局跳动。

### 6. 顶栏改为可换行/可折叠（对应 P1-2）

`MainWindow.xaml:59` 的横向 `StackPanel` 换成 `WrapPanel`，或窄屏时隐藏「组3：用户登录状态」的次要字段（会话剩余时间）。

### 7. 补 DPI 声明（对应 P2-1）

在 `MainAPP.csproj` 显式声明，并在目标机统一 100% 缩放：

```xml
<ApplicationHighDpiMode>PerMonitorV2</ApplicationHighDpiMode>
```

---

## 四、目标机自测清单

在 1024×768 机器上按顺序验证：

1. 首次启动（无 `Saves/Settings/settings.json`）→ 确认窗口是否被切成两半、左侧导航是否可见。
2. 逐个点开 7 个主导航页 → 确认左侧 Tab 条最后一项「关于」是否可见可点。
3. 打开「配方详情」（RecipeWindow）→ 确认底部按钮是否可见可点。
4. 从数据页打开「图表分析」（ChartsView）→ 尝试拖拽缩小窗口，确认能否缩到屏幕内。
5. 主页右侧「推理结果」表格 → 确认 9 列的实际可见列数。
6. 日志页选中一条日志 → 观察右侧抽屉出现后表格是否被挤到必须横向滚动。
7. 若现场缩放为 125%/150%，先改回 100% 再重复以上步骤对比。

---

## 五、结论

在 1024×768 @100% 缩放下：

- **主窗口最小宽度被硬编码为 1024**，等于屏幕宽度、零余量，默认尺寸 1200×800 必然溢出屏幕，且**全项目无屏幕尺寸钳制**——这是最需要优先修复的一项。
- **ChartsView、RecipeWindow 两个独立窗口的最小尺寸（1200/800、1024/680）本身就大于等于屏幕**，属于结构性不可用。
- 内容层面（HomeView 右栏、LogView 抽屉、顶栏状态组）在 934×620 的可用空间下会明显拥挤，但尚可通过滚动操作。
- 若目标机缩放为 125% 及以上，问题会从"勉强可用"恶化为"完全不可用"。

---

## 六、修复记录（2026-09-15 当日完成）

### 改动清单

| 文件 | 改动 |
|---|---|
| `MainAPP/Services/WindowSizing.cs` | **新增**。窗口尺寸夹取工具（纯函数 `Fit` + `Apply` / `ApplySizeOnly`），把「期望尺寸」夹到 `[900×600 安全下限, 当前显示器工作区]`。两处重复的 `Math.Max(1024/700)` 硬编码收敛到此处 |
| `MainAPP/MainWindow.xaml` | `MinWidth 1024→900`、`MinHeight 700→600` |
| `MainAPP/MainWindow.xaml.cs` | `ApplyWindowSettings` 改用 `WindowSizing.Apply`；新增启动日志记录「期望 vs 实际应用」尺寸，便于现场比对 |
| `MainAPP/Services/NavigationService.cs` | `ApplyWindowSettingsAsync` 改用同一套 `WindowSizing.Apply`，消除重复下限定义 |
| `MainAPP/Views/ChartsView.xaml` | `MinWidth 1200→900`、`MinHeight 800→600` |
| `MainAPP/Views/ChartsView.xaml.cs` | 构造函数按工作区夹取 XAML 声明的 1600×1000 |
| `MainAPP/Views/RecipeWindow.xaml` | `MinWidth 1024→900`、`MinHeight 680→600` |
| `MainAPP/Views/RecipeWindow.xaml.cs` | 构造函数按工作区夹取 XAML 声明的 1200×800 |
| `MainAPP/Views/LoginWindow.xaml.cs` | 仅夹取尺寸（`ApplySizeOnly`），不动其原有最小尺寸策略，覆盖 125% 缩放场景 |
| `MainAPP.Tests/Services/WindowSizingTests.cs` | **新增**。15 个单元测试，含 1024×728 工作区、极端小屏、不变量校验等 |

### 验证结果

| 验证项 | 结果 |
|---|---|
| 编译（MainAPP + 全部依赖） | ✅ 0 警告 0 错误 |
| `WindowSizingTests` | ✅ 15/15 通过 |
| 完整测试套件 | ⚠️ 678 通过 / 2 失败（**既有失败**，见下方说明，与本次改动无关） |
| 运行（工作区 1024×728，真实小屏） | ✅ 日志：`期望 1200×800 → 应用 1024×728`，窗口完整落在工作区内 |
| 运行（受控实验：期望值改为 2000×1400） | ✅ 日志：`期望 2000×1400 → 应用 1600×860`，夹取逻辑确认生效 |
| 无回归（工作区 1600×860） | ✅ 日志：`期望 1200×800 → 应用 1200×800`，大屏下尺寸不受影响 |
| 启动后功能自检 | ✅ 进程存活、主窗口「ABB机器人」响应正常、5000/5188/5190/5191 均监听、三个 Web 门户 HTTP 200 |

### 未处理项（需决策，本次未改动）

**既有测试失败 ×2（`ProtocolTemplateRendererTests`）** —— 与本次修复无关，`git stash` 撤掉本次全部改动后同样失败，可复现。

- `Render_BasicPlaceholders_FormatsNumbers`：期望 `12.345 → "12.35"`，实际 `"12.34"`
- `Render_CustomDecimalPlaces_UsesSuffix`：期望 `1.2345 → "1.235"`，实际 `"1.234"`
- 根因：`MainAPP/Services/ProtocolTemplateRenderer.cs:112` 使用 `Math.Round(v, decimals)`，默认 `MidpointRounding.ToEven`（银行家舍入），与测试注释声明的"四舍五入"不一致。
- **注意**：改成 `MidpointRounding.AwayFromZero` **也修不好**。因为 `12.345` / `1.2345` 在 IEEE 754 double 中本就略小于中点（`12.345` ≈ `12.34499999999999975`），需要改用 `decimal` 解析或显式处理，属于协议下发数据的语义变更，会影响发给机器人的坐标值，**需业务确认后再改**。

### 仍待处理（本次未做）

- P1-1 HomeView 右栏 3*:2* 在窄屏的列宽下限 / 比例切换
- P1-2 顶栏横向 StackPanel 改 WrapPanel
- P1-3 LogView 300px 抽屉改覆盖式浮层
- P1-4 SettingsView 通讯协议页 340px 固定左栏
- P2-1 `ApplicationHighDpiMode` 显式声明 + 目标机统一 100% 缩放

---

## 七、数据存储位置改造（2026-09-15 当日完成，与分辨率无关的第二项独立改造）

### 需求

数据落盘到 `D:\ProgramData\ItemBinding\`；无 D 盘时回退 `C:\ProgramData\ItemBinding\`；**不改动系统 `%ProgramData%`**。

### 实现

| 文件 | 改动 |
|---|---|
| `MainAPP/Services/DataPaths.cs` | **新增**。统一数据根解析：环境变量覆盖 → `D:\ProgramData\ItemBinding` → `C:\ProgramData\ItemBinding` → exe 目录兜底。要求本地固定磁盘 + 真实写入探针验证（不只看目录存在）。**不读写任何注册表或系统配置** |
| `MainAPP/Services/DataRootMigrator.cs` | **新增**。把 exe 目录下的历史数据（`DataBase` / `Saves` / `logs`）**合并复制**到新数据根，校验源中每个文件在目标存在后，把原目录改名为 `*.migrated` 保留可回滚 |
| `MainAPP/App.xaml.cs` | 启动时最先执行数据根解析 + 迁移（必须先于日志初始化，否则会拷到被占用的 SQLite 文件）；日志/配方路径改用 `DataPaths`；输出 `[数据根]` 日志便于现场核对 |
| `Models/Settings.cs`、`Settings/StorageSettings.cs`、`AppDbContext.cs`、`LogDbContext.cs` | 设置文件、图片目录、两个数据库路径改用 `DataPaths` |
| `Services/` 下 `LogDatabaseService`、`DataRetentionService`、`AuthService`、`ReportService`、`TemporaryLog`、`AI/KnowledgeBase`、`AI/AiChatService` | 同上 |
| `Services/LicenseService.cs` | 授权文件从 `%LOCALAPPDATA%` 迁到数据根（消除作用域错配）；**旧位置保留为只读回退**，老机器已激活的文件仍被识别 |
| `ViewModels/SettingsViewModel.cs` | 图片目录"恢复默认"指向数据根 |
| `MainAPP.Tests/TestDatabaseReset.cs` | **关键**：模块初始化时把数据根隔离到测试 bin 下的 `TestDataRoot`，避免集成测试误操作真实数据 |
| `MainAPP.Tests/Services/DataRootMigratorTests.cs` | **新增**。8 个测试覆盖迁移、回滚保留、不覆盖、部分目标、幂等、同根等 |
| `MainAPP.Tests` 下 `TemporaryLogIntegrationTests`、`LogDatabaseServiceIntegrationTests` | 断言路径改用 `DataPaths`（原先硬编码的是旧契约） |

**未纳入数据根**：`Models\best.onnx`（YOLO 模型）与 `cuda_runtime_dlls\`（CUDA 运行时）属随包分发的部署资产，仍走 exe 目录。

### 过程中发现并修复的严重缺陷

迁移逻辑最初写成"目标目录已有任何文件就整目录跳过"。这是**错的**：`Saves` 下同时有 `DataBase` / `Recipes` / `Security`，若目标只剩一个残留的 `Logs.db` 就跳过整个 `Saves`，**配方和账号会被漏搬、用户数据凭空丢失**。已改为**合并复制**——只补目标缺失的文件、绝不覆盖目标已有文件、逐个校验存在性后才改名。并补了对应回归测试（`MigrateIfNeeded_PartiallyPopulatedTarget_MigratesRemainingEntries`）。

另一个必须处理的副作用：`TestDatabaseReset` 的原始安全前提是"只作用于测试 bin 目录"，数据根改为按机器解析后，集成测试会直接读写并**删除真实生产库**。已通过环境变量覆盖把测试数据根隔离出去。

### 验证结果

| 验证项 | 结果 |
|---|---|
| 编译 | ✅ 0 警告 0 错误 |
| 完整测试套件 | ⚠️ 686 通过 / 2 失败（仍为既有的 `ProtocolTemplateRendererTests`，与本次无关）；新增 8 个迁移测试全绿 |
| 首次运行（exe 目录有旧数据） | ✅ `[数据根] D:\ProgramData\ItemBinding \| 数据根 = D:\ProgramData\ItemBinding（D: 可用）`；历史数据 3 项全部搬运，原目录改名 `*.migrated` |
| 第二次运行 | ✅ 日志无重复搬运记录，**幂等** |
| exe 目录 | ✅ 不再生成 `DataBase` / `Saves` / `logs` |
| 配方路径 | ✅ `配方管理器初始化完成，路径: D:\ProgramData\ItemBinding\Saves\Recipes` |
| 测试隔离 | ✅ 真实数据根时间戳未被 18:32 的测试轮次改动；测试数据落在 `MainAPP.Tests\bin\...\TestDataRoot` |

### 现场升级注意事项

1. **首次启动会自动搬运历史数据**，原数据改名保留为 `DataBase.migrated` / `Saves.migrated` / `logs.migrated`。确认新根数据正常后，这些可手工删除；若要回滚，把 `.migrated` 改回原名即可。
2. **搬迁发生在任何数据库打开之前**，所以不会拷到被占用的 SQLite 文件。但迁移期间请勿同时运行两个程序实例。
3. 可用环境变量 `ITEMBINDING_DATA_DIR` 覆盖数据根（部署脚本批量指定、或临时排查用）。
4. **授权文件位置变了**：老机器上 `%LOCALAPPDATA%\ItemBinding\license.txt` 仍能被读取（回退），但下次激活会写入数据根。当前 `ActivationRequired=false`，趁门禁关闭迁移成本最低。
5. 若目标机 D 盘是光驱/U 盘/网络盘（非本地固定磁盘），程序会自动跳过 D 盘直接用 C 盘，并在日志的 `[数据根]` 行说明原因。

### 7.1 数据库布局统一（同日追加）

**问题**：`barcode_data.db` 在数据根的 `DataBase\`，而 `Logs.db` 在 `Saves\DataBase\` —— 同类数据库分在两个层级，排查与备份时容易找错。

**方案**：统一到 `Saves\DataBase\`（`Saves` 本就是应用数据的总目录，`Logs.db` 已在此，改动量最小）。

| 项 | 改动前 | 改动后 |
|---|---|---|
| 业务库 | `<根>\DataBase\barcode_data.db` | `<根>\Saves\DataBase\barcode_data.db` |
| 日志库 | `<根>\Saves\DataBase\Logs.db` | 不变 |

**实现**：`DataPaths.DatabaseDir` 改指 `Saves\DataBase`，删除重复的 `LogDatabaseDir`（原先两个属性指向不同层级）；新增 `DataPaths.ObsoleteDatabaseDir` 仅供迁移使用。`DataRootMigrator` 增加两段逻辑：

1. **跨根搬运带目标映射** —— 旧根的一级目录不再同名搬运：`DataBase` → `<新根>\Saves\DataBase`，`Saves` → `<新根>\Saves`，`logs` → `<新根>\logs`。
2. **同根内旧布局归一** —— 数据根下若存在旧布局的 `DataBase\`（已跨根迁移过的机器），合并进 `Saves\DataBase\` 后退役为 `DataBase.migrated`。同根兜底分支（数据根 = exe 目录）也会走这段。

**验证（真实文件端到端）**：

| 步骤 | 结果 |
|---|---|
| 全新启动（数据根为空） | ✅ 直接生成统一布局，数据根下无 `DataBase\` |
| 人为构造旧布局（6 个文件移回 exe 目录并删除数据根），再启动 | ✅ 日志 `DataBase(3个文件) → D:\ProgramData\ItemBinding\Saves\DataBase`，两个库同目录，旧目录退役为 `*.migrated` |
| 数据完整性（md5 比对） | ✅ `barcode_data.db` 与 `users.json` 指纹与迁移前**完全一致**；`Logs.db` 变化属正常（启动时追加了本次日志） |
| 账号数据存活旁证 | ✅ 登录用户仍为迁移前的 `abb` |
| 测试 | ✅ 迁移测试由 8 个增至 10 个，全绿；完整套件 688 通过 / 2 失败（仍为既有 `ProtocolTemplateRendererTests`） |

**注意**：`Saves\` 之下的 `Recipes` / `Security` / `Settings` / `Pictures` 等均未变动；`<根>\logs\`（txt 日志）也保持原位，本次只统一了两个数据库。

### 7.2 清理记录

已清除本次改造过程中产生的全部测试数据（共 16 个文件，均为 2026-09-15 当天生成，不含任何真实业务数据）：`DataBase.migrated` / `Saves.migrated` / `logs.migrated`（exe 目录）、`D:\ProgramData\ItemBinding`（测试数据根）、`TestDataRoot`（测试隔离目录）。源码与报告未受影响。

---

## 八、既有缺陷修复：`App.OnExit` 后半段永不执行（2026-09-15 当日完成）

### 问题

`App.xaml.cs` 的 `protected override async void OnExit(ExitEventArgs e)`。**WPF 调用 `OnExit` 时不会 await 它**，因此命中第一个真正让出线程的 `await` 后，WPF 继续走完关闭流程并结束进程；该 await 的续体被投递到已在关闭的 Dispatcher 上，永远不会被调度 —— **第一个 await 之后的代码等于不存在**。

### 被跳过的实际影响

| 跳过的动作 | 后果 |
|---|---|
| `Settings.Instance.Save()` | **用户设置从不落盘**（窗口尺寸、密码、图片目录等改了重启就丢） |
| `BarcodeDataService.SavePendingAsync` | **待处理条码不保存**（追溯数据缺口） |
| 配方 TCP / 相机 Web / AI Web / 运维门户 / llama-server 侧车 停止 | 端口与子进程未正常释放 |
| 扫码枪 `AutoReconnect=false` + `Dispose()` + SDK 清理窗口 | **设备可能被驱动标记占用**（MVS 打不开，需等 SDK 会话超时）——即 2026-08-13 那次修复实际从未生效 |
| DI 容器释放、`MemoryDiagnostics.Shutdown` | 资源未释放 |
| `Log.CloseAndFlush()` | 最后一段日志丢失 |

### 判定为既有缺陷的依据

| 证据 | 结论 |
|---|---|
| `git show HEAD:MainAPP/App.xaml.cs` 第 166 行即为 `protected override async void OnExit(...)` | 改动前就是这个签名 |
| `git diff MainAPP/App.xaml.cs` 的 31 处改动 | **完全未涉及** `OnExit` / `Settings.Save` / `CloseAndFlush` |
| 当天多轮运行（含改造前的 17:30–17:51） | exe 目录下只出现过 `DataBase`/`Recipes`/`Security`，**从未出现过 `Settings`** |
| 受控优雅退出测试 | 日志无 `应用程序退出`、无 `settings.json` |

### 修复内容

1. **`OnExit` 由 `async void` 改为同步方法 + 有界阻塞等待**：11 处 `await X.WaitAsync(T)` → `X.WaitAsync(T).GetAwaiter().GetResult()`，`await Task.Delay(500)` → `Thread.Sleep(500)`；方法体用局部 `#pragma warning disable VSTHRD002` 包裹（与 `OnStartup` 中既有同类 pragma 一致）。
2. **`Settings.Save()` 提前到方法开头同步执行**（`SaveCriticalStateBeforeShutdown()`）：即使后续清理被超时截断，用户配置也已落盘。
3. **日志兜底**：`InitializeLogging()` 内注册 `AppDomain.CurrentDomain.ProcessExit` 刷 `Log`；`TemporaryLog` 静态构造函数内注册同款（仅在该类型确实被使用时注册，避免平白生成空临时日志文件）。

**为何同步阻塞是安全的**（这是初判"有死锁风险"后的修正）：等待对象全是后台 Task，且 `MemoryDiagnostics.RunPeriodicSnapshotAsync:239` 与 `DataRetentionService.RunPeriodicCleanupAsync:129` 内部**已经使用 `ConfigureAwait(false)`**，续体不回调 Dispatcher；所有等待均有超时上限，个别任务卡住也不会永久挂起。退出期间界面本不需响应，阻塞可以接受。

### 验证结果

| 验证项 | 修复前 | 修复后 |
|---|---|---|
| 日志出现 `应用程序退出` | ❌ 从未出现 | ✅ 出现（清理跑到最后） |
| `[退出] 设置已保存` | ❌ 无 | ✅ 有 |
| `Saves\Settings\settings.json` | ❌ 从不生成 | ✅ 已落盘，时间戳随退出更新 |
| 退出清理耗时 | — | **0.5 秒**，零超时噪声 |
| exe 目录被误写 | ✅ 无 | ✅ 无 |
| 完整测试套件 | 686 通过 / 2 失败 | **688 通过 / 2 失败**（仍仅既有 `ProtocolTemplateRendererTests`） |

### 验证手法（可复用）

`MainWindow.OnClosing` 有二次确认弹窗（"确定要退出应用程序吗？"），会挡住自动化关闭。做法是**临时**把 `OnClosing` 改为只调 `base.OnClosing(e)`，验证后立即还原，并用 `git diff MainAPP/MainWindow.xaml.cs | grep OnClosing` 确认无残留。发送关闭信号用 `Get-Process MainAPP | % { $_.CloseMainWindow() }`（无需 `Add-Type`，PostMessage WM_CLOSE）。
