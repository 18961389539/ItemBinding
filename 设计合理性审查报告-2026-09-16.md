# ItemBinding 设计合理性审查报告

- 审查日期：2026-09-16
- 审查对象：`D:\ItemBinding`（HEAD = `8dd9987`）
- 审查范围：MainAPP（132 个 .cs / 32,452 行）为主，含 JinlongYolo、HikCameraLib、LicenseSystem、ImageViewerControl 的关联设计
- 审查维度：架构与分层 / 启动与 DI / 数据持久化 / 配置与状态 / 账号与授权 / 并发与线程 / 硬件与 AI 推理链路 / 资源与内存 / 对外网络协议 / UI 数据流 / 工程化交付
- 方法：4 路并行源码审查 + 关键断言逐条回源核验（下述所有 P0/P1 均已用 `file:line` 复核）

---

## 一、总体判断

**这个项目的工程质量高于同类工控上位机的平均水平**——弱引用消息、全局异常三件套、配方原子写、Channel 反压、多级推理回退的冷却+冒烟验证、数据根优先级链，这些都是"想清楚了才写得出来"的设计。真正的测试套件 717 通过 / 0 失败，也不是摆设。

但作为**交付给产线的系统**，它的不合理集中在四个根因模式上，而不是一堆零散 bug：

| # | 根因模式 | 一句话 | 直接后果 |
|---|---|---|---|
| 1 | **信任边界缺失** | 把"调试/运维通道"和"生产控制通道"混在同一网络暴露面上，且四个端口全部零认证 | 车间任意一台电脑可切配方、改相机参数、驱动 AI 写库 |
| 2 | **缓存语义承载审计数据** | 用"攒批写"的缓存语义去存追溯数据，用"只增不减"去承载长周期生产 | 崩溃丢最多 499 条记录；数据库无限膨胀 |
| 3 | **DI 与静态单例双轨并行** | 容器只覆盖了后加的一半代码，另一半继续吃 `XxxService.Instance` | 领域逻辑不可单测、全局可变状态、数据流不可追踪 |
| 4 | **"能跑"被当成"可交付"** | 无 CI、无锁文件、SDK 不入库、门禁是编译期常量、schema 靠手工补列 | 新环境克隆后构建不出来；改一个 bool 才能开授权 |

下面按严重性列清单。

---

## 二、P0（上线前必须处理）

### P0-1 四个对外端口零认证，且默认绑定所有网卡

| 端口 | 服务 | 绑定 | 鉴权 | 危险动作 |
|---|---|---|---|---|
| 5000 | `RecipeTcpServerService` | `TcpListener(IPAddress.Any, 5000)`（:46） | **无** | `SWITCH_RECIPE` 切配方 |
| 5188 | `CameraWebHost` | `http://+:{port}/`（:202） | **无** | `/api/set` 暂停主检测循环并接管相机 |
| 5190 | `AiWebHost` | `http://+:{port}/`（:73） | **无** | `/ai/chat` 驱动 AI，若 `AllowWriteTools` 开启则可写库 |
| 5191 | `OpsWebHost` | `http://+:{port}/`（:48） | **无** | `/api/report`、`/api/ai/clear` |

三个 Web 宿主都是 `http://+` 优先、`127.0.0.1` 兜底（`CameraWebHost.cs:202-203`、`AiWebHost.cs:73/142`），只要执行过文档里的 `netsh http add urlacl`（`docs/部署_HTTP端口权限.md`），就等同于对整个车间局域网开放。

**为什么这是设计问题而不是配置问题**：代码本身把"局域网访问"写成了首选路径（注释和日志都在引导用户去开 urlacl，`CameraWebHost.cs:139/146`）。也就是设计意图就是暴露，只是漏了鉴权层。

**建议**：给四个端口加统一的最小鉴权（共享 token / Basic），并把"暴露面"降级为显式开关（默认仅 `127.0.0.1`，需要局域网时在设置里显式开启并强制填 token）。TCP 5000 额外加来源 IP 白名单。

---

### P0-2 检测记录攒够 500 条才落盘，崩溃即丢

```
MainAPP/Services/BarcodeDataService.cs:33   private const int BatchSize = 500;
MainAPP/Services/BarcodeDataService.cs:135  bool shouldFlush = pendingCountSnapshot >= BatchSize;
```

刷盘只有两个触发点：**累计满 500 条**，或**进程正常退出时** `SavePendingAsync`（唯一调用点 `App.xaml.cs:232`，位于 `OnExit` 内）。没有定时刷盘，没有每条异步落盘。

后果：断电 / 看门狗复位 / 任务管理器结束进程 / 未处理异常终止 → **最多 499 条检测记录永久丢失**。对一条"物码绑定 + 追溯"的产线来说，这就是"系统里查不到这条产品"的投诉来源。

**设计层面的不合理**：批量写本身没错（SQLite 逐条写确实慢），错的是把**持久化时机绑到"缓冲区大小"而不是"时间"**。追溯型数据必须有一个"最大未落盘时长"的上界。

**建议**：加一个 2–5 秒的定时 flush（`PeriodicTimer`），与 500 条阈值取先到者；`BatchSize` 同时降到 50–100 以缩小窗口。已有的 `_flushLock` 串行化机制可以直接复用，改动量很小。

---

### P0-3 账号体系名存实亡（三个设计选择叠加）

三条各自都不致命，叠起来等于没有门禁：

1. **口令明文存储**
   ```
   MainAPP/Models/AppUser.cs:43   public string Password { get; private set; } = string.Empty;
   MainAPP/Models/AppUser.cs:85   public bool VerifyPassword(string password)  → 明文比对
   ```
   代码注释（:9）自认明文，落盘在 `Saves\Security\users.json`。

2. **默认管理员是空口令，且启动免密自动登录**
   ```
   MainAPP/Models/Settings/SecuritySettings.cs:11  public string UserName { get; set; } = "abb";
   MainAPP/Models/Settings/SecuritySettings.cs:16  public string Password { get; set; } = string.Empty;
   MainAPP/Services/AuthService.cs:274             adminPassword = Settings.Instance.Password;  ← 空串
   MainAPP/Services/AuthService.cs:101             public bool AutoLoginDefaultAdmin()
   MainAPP/ViewModels/MainViewModel.cs:423-438     _isStartupAutoLogin = true; ...AutoLoginDefaultAdmin()
   ```
   启动路径直接建立 Admin 会话，且该会话不启动超时定时器（`MainViewModel.cs:170/295` 的 `_isStartupAutoLogin` 分支）。

3. **`users.json` 落在标准用户可写的目录**
   数据根为 `D:\ProgramData\ItemBinding\`（`DataPaths.cs` 优先级链）；`C:\ProgramData` 默认 ACL 允许 `Users` 建子目录并写文件。因此**直接编辑 `users.json` 给自己加一个 Admin 条目、或改 `Role` 字段**即可完成提权，连口令都不用碰。

**建议**：① 口令改 PBKDF2/bcrypt + 每用户 salt；② 默认管理员改为首次启动强制设置口令（或生成随机口令打印到首次启动日志），删掉免密自动登录或把它降级为"仅 Operator 只读会话"；③ 强烈建议把 `Security\` 单独放到受 ACL 保护的位置，或对 `users.json` 加哈希校验/机器签名，防止直接改文件提权。

---

### P0-4 授权门禁被硬编码关闭，且"关闭"是编译期决定

```
MainAPP/App.xaml.cs:145   private static readonly bool ActivationRequired = false;
MainAPP/App.xaml.cs:154   EnsureActivated(...)  → 直接 return true
```

`static readonly` 意味着**运行期无法恢复**，必须改源码 + 重新编译 + 重新发布。注释里写的是"临时停用"，但代码形态决定了它一旦发出去就是永久停用。

而且**当初关闭它的根本原因已经不存在了**：原注释说"数据/配方是机器级共享、授权文件是用户级（LOCALAPPDATA），换 Windows 账户即失配"——这个作用域错配已在 09-15 的数据根改造中消除（授权文件已迁入同一数据根）。

**建议**：把开关改成运行期配置项（配置文件 / 注册表 / 环境变量），默认开启校验；调试态用环境变量临时旁路，而不是改源码。同时注意 P1-13 提到的指纹含 `UserName` 的问题要一并修掉，否则重新开启后仍会有"换账户要求重新激活"的投诉。

---

## 三、P1（严重的可维护性 / 隐蔽正确性问题）

### 架构与耦合

| # | 问题 | 证据 |
|---|---|---|
| P1-1 | **Service Locator 与 DI 双轨并行**：`App.Services` 是全局容器（`App.xaml.cs:69`），`HomeViewModel` 构造函数用 `?? App.Services.GetRequiredService<...>()` 兜底（`HomeViewModel.cs:184-186`）；4 个 Web 宿主在 `App` 内直接 `new`，完全绕过容器（`App.xaml.cs:38-48`） | 渐进迁移中间态被固化 |
| P1-2 | **领域逻辑直接读全局单例**：`DetectionRecordService` 内 `Settings.Instance`（:244/295/560/574/722/725）、`RecipesManage.Instance`（:580/718）、`LicenseService.MachineCodeText`（:79）→ 纯计算逻辑被进程级可变状态绑死，不可单测 | `Application/DetectionRecordService.cs` |
| P1-3 | **分层反向依赖**：`Services → Application`（`Services/AI/AiChatService.cs:2` 引用 `MainAPP.Application`） | 当前未成环，但破坏依赖方向 |
| P1-4 | **服务层反向调 UI**：协议线程里 `Dispatcher.Invoke` 执行配方切换（`RecipeTcpServerService.cs:191-204`） | UI 忙时 TCP 应答延迟 |
| P1-5 | **拆分部类没有降低复杂度**：`HomeViewModel` 物理切成 5 个文件共 ~2,874 行，但跨分部直接互访的字段仍有 30+（`_inferenceLock`、`_productTracker`、`_processedFrameCount`、`ImageForShow`…） | 只是把上帝类切成 5 片，认知负担没降 |
| P1-6 | **超大方法**：`DetectionRecordService.BuildAndSaveAsync` 约 570 行；另 `AiChatService` 1552 行、`LogViewModel` 1132、`SettingsViewModel` 937、`RecipeViewModel` 832 | 单个方法内混合了配对、判定、落库、发送四件事 |

### 隐蔽正确性

| # | 问题 | 证据与后果 |
|---|---|---|
| P1-7 | **健康监控定时器启动即失效**：`_healthTimer` 只在 `OnCurrentRecipeChanged`（`HomeViewModel.cs:246-262`）里创建，而构造函数只**订阅**事件（:190）、不触发 | 若启动时已存在当前配方，定时器永远为 null → **丢帧率 / 绑定延迟 / 编码器链路状态栏永久不更新**，直到操作员第一次手动切配方。这是可复现的真 bug |
| P1-8 | **WAL 只初始化一次**：`_walInitialized` 是静态标志（`BarcodeDataService.cs:69-85`） | 库文件被删后重建时 `journal_mode` 退回 DELETE 且**永不重设** → 并发写性能与抗损坏能力静默退化（代码注释 M343a 自认此点） |
| P1-9 | **关键索引缺失**：仅 `DetectTime/Encode/Angle/BarcodeScore/Score` 单列索引（`AppDbContext.cs:64-76`） | 缺 `Barcode`（高频 LIKE 查询）、`RecipeName`、`(RecipeName, DetectTime)` 复合 → 追溯查询按全表扫，数据量上去后界面卡 |
| P1-10 | **schema 演进靠人肉补列**：`EnsureCreatedAsync`（:108）+ 十余处 `ALTER TABLE ... ADD COLUMN`（:128-213） | 没有 EF Migrations；下一版加字段仍要手写 ALTER + 判断列是否存在，漏一处即 INSERT 失败 |
| P1-11 | **AI 后端自愈只认一种异常**：仅捕获 `OnnxRuntimeException`（`HomeViewModel.cs:337-338`） | CUDA/cuDNN 以其它包装类型抛出的"会话建得起、跑不动"不会被判定为后端失效 → 每帧静默失败且永不自愈（`HealSegBackendAsync:377-456` 的冷却+冒烟验证机制是对的，只是入口没覆盖全）。**✅ 2026-09-16 已修**：判定抽为 `Application/InferenceBackendFailure.cs`，遍历 InnerException 链 + 原生后端特征词，并显式排除取消/已释放 |
| P1-12 | ~~**UI 线程同步阻塞设备**：`RecipeViewModel.Optimize.cs:90-97`、`CameraWebHost.cs:167`、`App.xaml.cs:561`~~ | **❌ 本条经复审为误报，予以更正**。逐点核实（2026-09-16）：① `Optimize.cs:90-97` 的 4 处 `GetAwaiter().GetResult()` **运行在 `Task.Run` 的线程池线程上**（模拟退火回调签名是同步的，`Task.Run` 已使其脱离 UI 线程，代码注释也已说明），不冻结界面；② `CameraWebHost.cs:167` 位于 `Stop()` 内，**唯一调用点是 `App.OnExit`**，属退出路径——本项目退出流程本就是"有界同步等待"的刻意设计；③ `App.xaml.cs:561` 是启动期且该任务确为已完成任务。**并且全项目 ViewModel 层对设备的调用全部是 `await ... ConfigureAwait(false)`（已逐条核对 `_scannerService.*`）**，不存在 UI 线程同步阻塞设备。真正存在的同类问题是"**后台线程同步阻塞等 UI 线程**"——见下条补充 |
| P1-12b | **协议线程阻塞式等 UI 线程**（复审补记）：`RecipeTcpServerService.cs:199/207` 用阻塞式 `Dispatcher.Invoke` | 全项目唯一的阻塞式 Dispatcher 调用。相机/配方页/弹窗占用 UI 线程时，TCP 应答被拖住；且"协议线程等 UI、UI 又等协议线程"存在死锁可能。**✅ 2026-09-16 已修**：`ProcessCommand` 改 `ProcessCommandAsync` + `await InvokeAsync`（语义不变：仍在配方真切换完后才应答），纯提示性 Toast 拆出等待链 |
| P1-13 | ~~**硬件指纹含 UserName**~~：`HardwareFingerprint.cs` = CPU ProcessorId + 系统盘序列号 + 首物理网卡 MAC + MachineName + ~~**UserName**~~ | 换 Windows 账户即导致授权失效——正是当初被迫关掉门禁的同一类问题。**✅ 2026-09-16 已修**：UserName 移出当前算法，旧算法保留为 `ComputeLegacyFingerprintBytes` 供校验回退（避免已部署机器的激活码全部作废），`LicenseService.ValidateCode` 仅在 MachineMismatch 时回退旧指纹并打 Warning 提示换发新码。**实机验证**：本机旧激活码经旧指纹路径成功放行。残留（未改）：换网卡 / 换 CPU / 重装系统仍会失效——属指纹的固有取舍，需另行决策 |
| P1-14 | ~~**运维门户一次异常即永久失效**~~：`OpsWebHost.AcceptLoopAsync` 的 `catch (Exception) { break; }` | 任何意外异常直接退出接收循环且**无重启**，运维门户到下次重启程序前都不可用。**✅ 2026-09-16 已修**（两道防线）：① 异常分类对齐另两个宿主（取消 / 已释放 / 监听器关闭 → 正常收尾；其余记 Warning 后 `continue`）；② 抽出可测的 `Services/AcceptLoopSupervisor`——循环意外退出而服务仍应运行时按指数退避（500ms→10s）自动重启并记日志，`Stop/Dispose` 通过运行标志退出、不会误重启。**实机验证**：门户 `/` 与 `/api/ops` 均 HTTP 200，优雅退出时重启告警 0 条 |

### 状态、资源与数据增长

| # | 问题 | 证据 |
|---|---|---|
| P1-15 | ~~**配置只在退出时保存**~~（准确说：只有设置页 / 切配方 / 退出**三处**显式调 `Save()`） | 任何"改了属性但没调 Save()"的路径在崩溃或断电时静默丢失。**✅ 2026-09-16 已修**：新增 `Settings.StartPeriodicPersist()`（60s 周期）调 `TryFlushToDiskIfChanged()` —— 内容有变化才原子写盘、且**不发 Changed 事件**（纯持久化刷新不等于"设置被改动"，否则订阅方每周期被无意义触发）；最长丢失窗口从"下次显式 Save"收敛到 60 秒。启停分别接在 `App.ReloadSettingsAndStartCleanup` 与 `SaveCriticalStateBeforeShutdown`。**实机验证**：空闲运行 80s 未产生冗余写盘（mtime 不变），退出时仍正常落盘 |
| P1-16 | ~~**`Settings.Instance` 双重检查无锁**~~：原 `if (_instance == null) _instance = Load();` | 多线程首访会各自 Load 出不同实例，后写覆盖先写，先拿到的调用方握着被丢弃的实例。**✅ 2026-09-16 已修**：改用 `Lazy<Settings>(Load, ExecutionAndPublication)`（只执行一次、各线程同一实例）；并新增 `_persistLock` 串行化 `Save`/`Reload`（否则 Save 可能把"重载到一半"的中间态落盘，或两个 Save 抢 `File.Replace`）。**实机+单测验证**：64 线程并发取 Instance 全部同一引用 |
| P1-17 | **业务库只增不减**：`DataRetentionService.cs:43-52` 仅按时间/数量删行，**业务库无 VACUUM**、无归档、无分表；`DataRetentionService.cs:180` 的裸连接无 `busy_timeout`；`LogDbContext.cs:31` 也无 `busy_timeout` | 库文件只增不缩、磁盘膨胀、查询持续变慢；清理与写入并发时可能 `SQLITE_BUSY` |
| P1-18 | **协议无长度/连接上限**：`RecipeTcpServerService.cs:162` 逐字节 `Append` 且每字节刷新 0.8s 截止期，无最大行长；`:90` 每连接 `Task.Run` 无上限 | 慢速客户端可让内存持续增长；连接数无节制 |
| P1-19 | ~~**`s_isPaused` 无持有者语义**~~ | 三方各自手写"读 IsLoopPaused → 未暂停则 PauseLoop → finally 恢复"的防踩踏逻辑：① 读-改-写非原子，两方可能同时认为"不是我暂停的"→ 都暂停都恢复；② 无法回答"谁把循环停住了"。**✅ 2026-09-16 已修**：抽出 `Services/MainLoopGate`（**按持有者记账**：同一持有者重入只算一次、`Resume(owner)` 只能释放自己那份、他人的持有不受影响；`Describe()` 给出"谁持有、已持有多久"）；三处调用点的 `wasPaused` 手写逻辑与 `CameraWebHost._pausedByHost` 全部删除；运维门户 / 相机状态 JSON 新增 `pausedBy` 字段——排查"主循环不动了"时直接看到是谁停的。**实机验证**：`/api/ops` 返回 `paused=False, pausedBy=未暂停` |

### 工程化交付

| # | 问题 | 证据 |
|---|---|---|
| P1-20 | ~~**无 CI**~~：仓库无 `.github/workflows` | 没有自动构建/测试门禁。**✅ 2026-09-16 已修**：新增 `.github/workflows/ci.yml`（windows-latest；装 .NET 8 运行时 + 10 编译器；缓存 NuGet；恢复→构建→测试）。因厂商 SDK 不入库，CI 分两条路径：必过的是「厂商无关子集」`ItemBinding.SdkFree.slnx`（Blob/ImageViewerControl/JinlongYolo/LicenseCore/ScottPlot + 两个测试工程），厂商 SDK 就位时才额外构建完整解决方案。**顺带修掉一个静默缺口**：`LicenseCore.Tests` 原先不在任何解决方案里 → `dotnet test ItemBinding.slnx` 会跳过它，授权逻辑从未被自动跑到；现已纳入 SdkFree（CI 必跑，36 个用例）。**实机验证**：子集 187+36 通过；完整 187+767 通过 |
| P1-21 | ~~**不可重现构建**~~：无 `Directory.Packages.props` | 同一包在不同工程出现多个版本（实测 **7 个包冲突**：OpenCvSharp4 两版、DI 8.0.1/10.0.1、Test.Sdk 17.11.1/17.14.1、xunit 两版等）。**✅ 2026-09-16 已修**：引入中央包版本管理（CPM）——`Directory.Packages.props` 是**唯一**版本来源，17 个 csproj 的内联 `Version` 全部剥离；冲突按多数派裁决并在文件内写明依据；vendored 的 `ScottPlot5`/`ScottPlot.WPF` 就近关闭 CPM（其版本按 TFM 分支，纳入反而易错）。**实机验证**：0 警告 0 错误，767+187+36 全通过（统一版本后无回归）。**残留（有意未做）**：未启用 `packages.lock.json` 锁定传递依赖——CPM 只锁直接引用，锁文件才能锁住整棵树；但开启后每次改包都要重新生成锁文件，属独立决策 |
| P1-22 | **Nullable 只开了主工程**：`MainAPP.csproj:6` 已 `enable`，但 `Directory.Build.props:13` 故意不统一，其余库未必开启；`TreatWarningsAsErrors` 未设 | 这个规模的项目，未开 Nullable 的库是 NRE 温床 |
| P1-23 | ~~**`*.dll` 被 gitignore 排除**~~（`.gitignore:7`） | 海康 SDK 原生 DLL（**编译期必需**）、`cuda_runtime_dlls/` 均不入库 → 新克隆手工恢复，且症状是几十条 CS0246（看不出真因）。**✅ 2026-09-16 已修（保持不入库，但把机制补全）**：① 新增 `HikCameraLib/HikScanner/DLLs.sha256`（25 个文件的 SHA256 清单，与既有 CUDA 清单同一范式）；② 新增 `scripts/verify-vendor-dlls.ps1` 一键校验收到的 SDK 版本是否与本仓库一致；③ `HikScanner.csproj` 加构建闸门：SDK 缺失时抛 **HIKSDK001** 并给出恢复步骤（实测 CS0246/MSB3245 归零）；④ `ItemBinding.SdkFree.slnx` 让无 SDK 环境也能构建+测试。**为什么不入库**：仓库是**公开仓库**（API 返回 `"private": false`），海康 SDK 属第三方授权件，再分发有许可风险 —— 这一步需要你拍板 |

---

## 四、P2（改进项）

**数据与配置**
- CSV 导出用 `Encoding.UTF8` **无 BOM**（`BarcodeDataService.cs:724/750`）→ Excel 打开中文乱码。
- `ReportService.cs:192-194` 的 HTML 转义只处理 `& < >`，未处理引号（当前都在文本上下文，风险低但不完整）。
- 配方无 schema 版本号，靠 `GrabOffsetMigrated` 这类**布尔 flag 位**做迁移（`Recipe.cs:59/125-147`）→ 每加一个迁移字段就要加一个 flag，不收敛；建议改为 `SchemaVersion` 单调整数。
- `Recipe.LoadFromFile` 失败返回 `null`（`Recipe.cs:168-173`），上层可能用默认配方**覆盖现场配置**——损坏时应拒绝加载并报错，而不是静默降级。
- 授权限时码可被**系统时间回拨**绕过（`LicenseValidator.cs:87-94`），建议记录"最大已见时间"做单调校验。

**并发与实时性**
- `NeedDrop` 帧虽然跳过落盘，但仍占用推理信号量与处理槽（`Process.cs:98-105`）→ 队列拥挤时降不了压；且 `Channel` 用的是 `FullMode.Wait`（`MainLoop.cs:459-464`），与注释里"由 NeedDrop 实现丢帧"的说法语义矛盾。
- `ReadImage.cs:236-242` 三个 `catch` 直接 `return`，**未 `Dispose` 已从 ArrayPool 租出的 `FrameResult`**（约 2.6 MB 的 `ImageData`，`FrameResult.cs:87`）→ 只发生在取消/关闭路径，属 P2，但确实泄漏。
- `AiChatService` 的会话历史经核查来自**有界 DB 查询**，未发现内存无限增长（"AI 会话历史泄漏"这条不成立）。
- 每帧多次读 `Settings.Instance` 与 `RecipesManage.Instance.CurrentRecipe`（`Process.cs:147`、`Draw.cs:112-116`）→ 单例属性读开销叠加在热路径上。
- **角度模型推理失败逐帧写 Error**（`DetectionRecordService.TryComputeAngleWithModelAsync` 的 catch）：角度推理每帧每目标调用一次，失败时每秒数条 Error → 淹没真正有用的日志、给日志 IO 添负担。**✅ 2026-09-16 已修**：限频聚合（30s 一条，带窗口内次数 + 是否后端失效 + 最近异常），节流逻辑抽为可测的 `Application/AggregateLogThrottle`（`RecordAngleReject` 也一并改用它）。同时查明：**角度池固定纯 CPU 是刻意设计**（因角度链路无运行期自愈，规避 CUDA 会话级崩溃），故角度侧不存在"需自愈而未自愈"的问题——该结论已写入代码注释，防止后人误开 CUDA。
- `HomeView` 日志区用 `CollectionView` 的 `SortDescriptions` 做倒序（`HomeView.xaml:332`），每次 Add 触发整集合排序；`LogsCount` 调大后 UI 线程开销显著。

**UI 与可用性**
- ~~推理后端切换**只在日志和通知里可见**，状态栏无"当前推理后端"指示~~ → **✅ 2026-09-16 已修**：HUD 增加"推理后端"一行（`HomeViewModel.Display.cs` 的 `InferenceBackendDisplay` + `IsInferenceDegraded`，降级时转琥珀色 `#EF9F27`；更新入口唯一化为 `SetInferenceBackend`，后台线程自动封送到 UI 线程）。
- 抓取点：帧日志未记录当次生效偏移量；抓取点落在矩形外无提示（09-15 已记录，仍未做）。
- 硬件**无接口抽象**（无 `ICamera`/`IScanner`，`Devices.Scanners.HikScaner` 单例直连，`ReadImage.cs:98`、`MainLoop.cs:87`）→ 主循环取帧、推理调度、相机软触发切换这些关键路径**完全无法自动化测试**。这是当前测试覆盖率的真正天花板。
- 无 `app.manifest`、无 `dpiAware` 声明（已核验 exe 内嵌清单只有 `asInvoker`）→ 125% 缩放下有效 DIP 空间被压缩；09-15 的 `WindowSizing` 已兜住溢出，但根因仍在。
- `RecipeViewModel` 里直接编排相机（`Optimize.cs`）、`HomeViewModel` 自建 `CoordinateTransformer`/`ProductTracker` → "VM 只经服务访问硬件"的约定在本项目从未成立。
- `RecipeTcpServerService` 端口未读 Settings（注释"后续可配置"，`:23`），是硬编码。
- `HomeView` 的 `InferenceDataGrid` 未显式声明虚拟化（默认开启，当前数据量小），`ChartsView` 同样未强制。
- ~~`docs/部署_HTTP端口权限.md` 与 `scripts/install-urlacl.cmd` 只覆盖 5188/5190，**漏了运维门户 5191**~~ → **✅ 2026-09-16 已补**：两处均加入 5191 并写明『三条记录』的验证预期；原缺口会让照文档部署的现场把 5191 静默绑在回环上（手机打不开），而文档看起来是『按步骤做完了』，极难自查。

---

## 五、明确**合理、不要动**的部分

这一节同样重要——避免整改时把好设计一起改坏：

| 设计 | 证据 | 为什么好 |
|---|---|---|
| 弱引用消息 + 显式注销 | `WeakReferenceMessenger`，`MainViewModel.cs:805` `UnregisterAll(this)` | 全项目最规范的子系统，无强引用泄漏 |
| `OnExit` 同步 + 有界等待 | `App.xaml.cs`（09-15 改造） | 修掉了"首个 await 之后代码永不执行"，且用 `ConfigureAwait(false)` + 超时避免了死锁 |
| 全局异常三件套 | `App.xaml.cs:703 / 710 / 743`，含 `StackOverflow/OOM/AV/SEH` 重抛 | 策略分级正确，不是无脑 `Handled=true` |
| 配方原子写 | `Recipe.cs:179-203`（`.tmp` + `Replace`） | 断电不会产生半个 JSON |
| Channel 反压 | `MainLoop.cs:459-464`，`Bounded(8)` + `Wait` | 队列满时反压到取帧端，不丢新帧——工业场景下正确的选择 |
| 多级推理回退 | 加载期逐级下沉；运行期连续 2 次失败 + 30s 冷却 + 防重入 + 64×48 冒烟验证后**原子换入**（`HomeViewModel.cs:350-456`） | 不是每帧重试 CUDA，是真正想过现场行为的实现（入口异常识别已于 2026-09-16 补全，见 P1-11） |
| 日志集合有界 + 高频过滤 + 虚拟化 | `LogService.cs:101-126`、`ShouldSkipUiLog:141-160`、`LogView.xaml:752-755` | 不是 `TextBox` 累加字符串的低级做法 |
| 数据根优先级链 + 真实写入探针 | `DataPaths.cs`（环境变量 → D: → C: → exe 兜底） | 探针设计避免了"解析出来但写不进去"的经典坑 |
| 迁移的合并复制 | `DataRootMigrator.cs`（只补缺失文件、不覆盖、逐个校验后才改名） | 避开了"目标有文件就整目录跳过"导致的漏搬 |
| 无反射反模式 / 无 SQL 拼接 / `#pragma` 克制 | grep 全量核验：命中均为 `JsonElement.GetProperty`（非反射）；`ExecuteSqlRaw` 全为常量 DDL/PRAGMA；33 处 `#pragma` 均为 VSTHRD 系列；源码无 TODO/HACK/FIXME | 这个规模能做到这个程度不容易 |
| `WindowSizing` 夹取 | `Services/WindowSizing.cs` | 纯函数可测，方向正确 |

---

## 六、建议的整改顺序

**第一批（上线前，估 2–3 人日）**
1. 四个端口加最小鉴权 + 暴露面收敛为显式开关（P0-1）
2. 写库队列加 2–5s 定时 flush（P0-2，改动最小、收益最大）
3. 密码哈希化 + 去掉免密自动登录 + 首次启动强制设口令（P0-3）
4. ~~`ActivationRequired` 改运行期配置 + 修掉指纹里的 `UserName`（P0-4 + P1-13）~~ —— ✅ 2026-09-16 已完成
5. 修 `_healthTimer` 启动失效（P1-7）——真 bug，一行改动

**第二批（投产稳定性，估 3–5 人日）**
6. 索引补齐 + 打开 Migrations + 业务库 VACUUM/归档策略（P1-9/10/17）
7. ~~AI 自愈入口扩展异常类型 + 状态栏显示当前推理后端（P1-11 + P2）~~ —— ✅ 2026-09-16 已完成
8. ~~`OpsWebHost` 接收循环加重试（P1-14）~~ —— ✅ 2026-09-16 已完成（另补：部署脚本与文档漏了 5191 的 ACL）
9. 协议加行长上限、连接上限、来源白名单（P1-18）
10. WAL 重建后重设、`busy_timeout` 统一（P1-8/17）

**第三批（可维护性，结构性）**
11. `ICamera`/`IScanner` 接口化 —— 这是解锁测试覆盖率的唯一途径（P2，但优先级实际很高）
12. `Settings` 改线程安全单例 + 定时保存；Web 宿主纳入 DI；`Settings.Instance` 从领域逻辑中移出（P1-1/2/15/16）
13. 拆 `BuildAndSaveAsync`；`LogViewModel`/`SettingsViewModel` 按职责拆（P1-5/6）
14. ~~补 CI + `Directory.Packages.props`~~（P1-20/21）—— ✅ 2026-09-16 已完成；**全局开 Nullable（P1-22）仍未做**
15. ~~硬件 SDK DLL 的恢复步骤写进交付文档~~（P1-23）—— ✅ 2026-09-16 已补清单+校验脚本+构建闸门；**若要走 CI 全量覆盖，需提供可访问的 SDK 构建资产**

---

## 七、一句话总结

**功能实现是想过现场的，工程基建是欠账的。** 最需要改变的不是任何一行代码，而是两个习惯：**对外暴露必须先回答"谁有权限调用"**，**追溯数据必须先回答"最多允许丢多久"**。这两条定了，剩下的都是可排期的活。
