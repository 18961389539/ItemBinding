# 灰度判向诊断报告

- 日期：2026-09-11
- 数据源：`MainAPP/bin/Release/net10.0-windows/publish/DataBase/barcode_data.db`（18001 条记录，2026-09-11 11:22 快照）
- 图像源：`D:\Pictures\jijing\` 下 22381 张 `draw_*.jpg`（= 推理图 = 原图 / 配方 ResizeScale 8）
- 分析脚本：`tools/brightness_diagnose.py`（库统计）、`tools/brightness_replay.py`（真实图回放）
- 相关配置：配方 `Saves/Recipes/jijing/jijing.recipe`（`ResizeWidth/Height = 8`，`IsEnableAngelDetection = false`，`OffsetAngle = -2`）

---

## 一、结论速览

1. **灰度判向在生产上目前基本处于失效状态**：`|BrightnessDiff|` 中位数只有 **1.57 灰度级**，而判向死区默认是 **5.0** —— **96.1% 的帧落在死区内，不会触发任何翻转**。灰度判向实际参与决策的比例约为 3.9%。
2. **根因不是判向算法写错了，而是三件事叠加**：
   - **物理反差本身弱**：深色印刷包装袋，两端明暗接近；
   - **图像动态范围只用了 47~107**（两侧平均灰度约 75/255，仅占满量程 29%），端部反差被压在低位区间；
   - **掩码长轴存在 ±180° 轴向不定**：同一趟通过内 **28.2%** 出现角度翻 180°，并连带把 `diff` 变号（83.6% 的翻转型趟印证）。
3. **只有极少数产品是可判的**：`|diff|` 分布有一个独立高值簇（442 条落在 12~20 区间），说明部分产品端部特征明确 —— 这批样本的判向是可靠的，其余大部分不是。

---

## 二、全库统计证据

### 2.1 数据概览

| 项目 | 数值 |
|---|---|
| 总记录数 | 18001 |
| 含判向统计的记录 | 18001（100%）——判向链路在跑，只是结果都被死区拦下 |
| 唯一条码数 | 7981（`noread` 仅 16） |
| 每产品平均帧数 | 2.3（最大 18） |
| 检测置信度 | p5 = 0.972，p50 = 0.985，p95 = 0.992 |

### 2.2 |BrightnessDiff| 分布

| 分位 | p1 | p5 | p10 | p25 | p50 | p75 | p90 | p95 | p99 |
|---|---|---|---|---|---|---|---|---|---|
| \|diff\| | 0.0 | 0.2 | 0.3 | 0.8 | **1.6** | 2.6 | 3.5 | 4.4 | 15.7 |

| 区间 | 样本数 | 占比 |
|---|---|---|
| [0, 1) | 5522 | 30.7% |
| [1, 2) | 5463 | 30.3% |
| [2, 3) | 4008 | 22.3% |
| [3, 5) | 2305 | 12.8% |
| [5, 8) | 199 | 1.1% |
| [8, 12) | 60 | 0.3% |
| [12, 20) | **442** | **2.5%** ← 独立高值簇 |
| ≥20 | 2 | 0.0% |

- 死区内（`|diff| < 5`）：**17298 / 18001 = 96.1%**
- 死区外：703 条，其中 `diff > 0` 290 条、`diff < 0` 413 条 —— 正负接近均衡，是"围绕零的噪声"的典型形态。

### 2.3 灰度水平（不是过曝）

| 指标 | 数值 |
|---|---|
| MeanPlus（+u 半区） | min 47.1 / 平均 75.7 / max 107.2 |
| MeanMinus（−u 半区） | min 46.9 / 平均 75.0 / max 105.7 |
| 半区均值 > 200 的样本 | 0（0.0%） |

**判读：不是过曝，反而是产品整体成像偏暗。** 产品掩码内灰度只覆盖 47~107 这 60 个灰度级，等于只用了满量程的 29%。在这么窄的区间里做两端比较，差值天然被压小 —— 这也是后面"线性拉伸"收益最大的直接原因。

### 2.4 产品几何

- 外接框长宽比：p5 1.23 / p50 **1.45** / p90 1.49 —— 接近方形的矩形包装袋
- 主要尺寸簇：约 1680 × 1140（原图像素），对应推理图约 210 × 143

---

## 三、趟内稳定性（按"同一趟通过"切分）

> 方法：按条码分组，相邻帧间隔 ≤ 1.5s 视为同一趟通过（同一物理产品连续成像）。全库共 8343 趟，其中 >=3 帧的 1666 趟（覆盖 4998 帧）。
> 这样切分才能把"操作员重新摆放导致的真实换向"与"算法抖动"分开。

| 指标 | 干净型趟（角度一致） | 翻转型趟（趟内翻 180°） |
|---|---|---|
| 趟数 | 1196（**71.8%**） | 470（**28.2%**） |
| 趟内 \|diff\| 中位 | 1.40 | 1.45 |
| 趟内 \|diff\| 标准差 σ | **0.71** | 1.61 |
| 噪声/信号比（σ / \|diff\|） | **0.52** | 1.02 |
| 趟内符号不稳定（一致率<80%） | 25.8% | 89.1% |
| 几何角抖动（排除翻转） | **p50 = 0.00°，p75 = 0.30°** | — |

**两条关键推论：**

1. **几何本身极其稳定**：干净趟内掩码长轴的角度抖动中位数是 **0.00°**（p75 仅 0.30°）。之前按"同条码跨帧"算出的 24°/55° 波动是**不同趟之间的真实重新摆放**，不是算法抖动。掩码质量（Score p50 = 0.985）也没问题。
2. **180° 翻转是独立的结构性缺陷**：几何角稳定，却有 28.2% 的趟在趟内把输出角度翻了 180°。而且翻转前后 `|diff|` 同时变号的占 **393/470 = 83.6%** —— 这说明**"角度翻 180°"与"diff 变号"是同一件事**：长轴轴向翻转把 +u/−u 两个半区对调了。

全库角度分布也印证了这一点，呈明显 **±90° 双峰**（`[-100,-90)` 9061 条、`[90,100)` 1740 条、`[80,90)` 1353 条）—— 这正是 180° 歧义没被消掉时该有的形态。

### 为什么"降死区"不是好办法

干净趟的 σ = 0.71、|diff| = 1.40，SNR ≈ 2。如果 `diff` 只是围绕恒定值的高斯噪声，单帧变号概率约 2.4%；**实测趟内符号不稳定率是 25.8%，高了约 10 倍**。说明 `diff` 在趟内有系统性漂移（产品运动中姿态微变、光照微变），不是纯噪声。这种情况下降死区会让角度更频繁地误翻转，得不偿失。

---

## 四、真实图回放验证（487 张实测图）

> 方法：从库中随机抽 250 条（实际成功回放 487 张，含同一记录多帧）→ 按配方 ResizeScale 定位检测框 → 在 ROI 内用"暗像素 + 凸包填充"重建产品区域 → PCA 求长轴 → 在同一区域、同一分割轴上对比各种度量方式。
> 标定：阈值取"传送带基准 +15 灰度级"时，重建的两侧均值与落库 `BrightMean/DarkMean` 偏差中位数最小（6.2 灰度级）。
> **重要限制**：回放重建的产品区域不是模型的分割掩码（偏差中位 6.2 级），因此**回放基线的 |diff| 中位数（4.49）高于落库值（1.57）**。绝对值不可直接套用生产，**可迁移的是"逐图增益比"**（同一张图上变体/基线的比值）。

| 方案 | \|diff\| 中位 | p90 | 超死区占比 | **逐图增益(中位)** | 增益>1 的图占比 |
|---|---|---|---|---|---|
| ① 基线：整半区灰度均值（当前实现） | 4.49 | 7.57 | 44.6% | 1.00× | — |
| ② 端点环带 30% · 灰度 | 5.85 | 9.37 | 56.9% | 1.25× | 78% |
| ③ 端点环带 12% · 灰度 | 5.86 | 11.22 | 59.3% | 1.25× | 67% |
| ④ 整半区 · 最佳通道（R/G/B/饱和度） | 5.16 | 8.16 | 52.4% | 1.08× | 100% |
| ⑤ 整半区 · 线性拉伸 (p1-p99) | 9.89 | 17.79 | 76.0% | **2.31×** | 100% |
| ⑥ 端点环带 30% · 线性拉伸 | **12.61** | 22.38 | **80.1%** | **2.82×** | 94% |
| ⑦ 整半区 · 线性拉伸 + 最佳通道 | 11.05 | 18.48 | 77.8% | 2.46× | 100% |
| ⑧ 端点环带 30% · 大尺度背景扣除 | 3.85 | 5.43 | 19.5% | **0.76×** | 35% |

### 判读

- **收益最大：掩码内线性拉伸（p1–p99 → 0–255），2.31×**，且 100% 的图都有效。这与 2.3 节"动态范围只用了 29%"完全对应，是最确定的一招。
- **叠加端点环带 30% 后到 2.82×**，超死区占比从 44.6% 提到 80.1%。环带宽度取 30% 优于 12%（中位相同但 p90 与稳定性更好）。
- **最佳通道单独收益很小（1.08×）**，但胜率 100% 且几乎零成本，适合作为免费叠加项。注意 1.08× 说明**颜色反差不是主因**——产品端的差异主要体现在明暗而非色相。
- **大尺度背景扣除是负收益（0.76×）**，19.5% 才超死区。**说明端部差异不是照明不均造成的，而是产品自身的大尺度明暗特征**；高通滤波恰好把这个信号滤掉了。这条应当明确排除。
- **端点环带单独只有 1.25×**：与最初"封口条只占端部一小段、被整半区平均稀释"的假设相比，实测增益低于预期。结合背景扣除的负收益看，**差异更像是沿长轴分布的渐变，而不是集中在最外端的一小块特征**。

---

## 五、建议执行顺序

### P0 — 先把轴向定死（不增加灰度差，但是前提）

28.2% 的趟内 180° 翻转是当前最严重的缺陷：**同一趟成像会输出两个相差 180° 的角度**，且它同时污染 `diff` 的符号（83.6% 印证），使判向统计失去意义。

修法：在 `JinlongYolo/Extensions/SegmentationExtensions.cs:BuildMinAreaRect`（已有"宽 ≥ 高"归一化）之后，再对长轴方向做**符号规范化**，把 `u` 强制到固定半平面（如 `ux < 0` 或 `ux == 0 && uy < 0` 时取反）。这样几何角唯一确定，不再依赖灰度判向去纠偏；灰度判向只需负责"头/尾"这一件事。

这一项应优先做，因为它让后续所有调参和统计变得可解释。

### P1 — 掩码内对比度拉伸（2.31×）

在判向前，用掩码内灰度的 p1/p99 构造线性拉伸（或查表 LUT），把产品动态范围拉满 0–255。生产侧实现成本低（掩码已经有了，百分位一次遍历即可）。

### P2 — 叠加端点环带采样（合并 2.82×）

带宽建议 30%，并对每侧设最小像素数保护（避免小掩码下只用几十个像素定方向）。

### P3 — 最佳通道（1.08×，免费叠加）

按掩码内 R/G/B/饱和度的最大反差选通道。收益小但无副作用。

### 不建议做

- **大尺度背景扣除**：实测 0.76×，负收益
- **单纯调低死区**：趟内 `diff` 存在系统性漂移，实测符号不稳定率 25.8%（远高于纯噪声预期的 ~2.4%），降死区会引入误翻转

### 硬件侧

- 曝光不是问题（无过曝），但**产品整体成像只落在 47–107**，说明这类深色包装袋本身反射率低。若现场允许，改用**单侧低角度定向光**打产品端部，比继续在算法上榨取更有效。
- 若产品批量大，**启用角度检测模型（两级分割）**是最彻底的方案：模型学的是语义特征，不依赖单一灰度阈值。当前配方 `IsEnableAngelDetection = false`，走的是掩码回退路径。

---

## 六、复现方式

```bash
# 库统计诊断
python tools/brightness_diagnose.py --db "<barcode_data.db 路径>"

# 真实图回放验证（需在配方 ResizeScale 匹配时指定 --resize-scale）
python tools/brightness_replay.py --db "<barcode_data.db 路径>" --images D:\Pictures\jijing \
    --n 250 --calib 40 --resize-scale 8 --out 灰度判向_回放验证报告.md
```

两个脚本都会先把 `.db / -wal / -shm` 快照到临时目录再只读打开，不影响正在运行的生产程序。
