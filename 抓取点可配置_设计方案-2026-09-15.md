# 抓取点可配置 — 设计方案

- 日期：2026-09-15
- 范围：`MainAPP`（检测链路 + 配方模型 + 配方界面）
- 目标：让抓取点不再固定为旋转矩形中心，且不改动现有产线行为（默认值与现状完全一致）

---

## 一、现状回顾

抓取点 = 掩码最小外接旋转矩形的几何中心（`DetectionRecordService.cs:309` 有明确注释）。
发送给机器人的 `WorldX/WorldY` 由它经三点标定转换后再加配方常量偏移得到。

现有可配置项（配方级，`Models/Recipe.cs`）：

| 项 | 单位 | 语义 |
|---|---|---|
| `OffsetX` / `OffsetY` | mm | **世界坐标系**常量平移，用于校正安装/标定残差 |
| `OffsetAngle` | ° | 角度偏移，叠加在掩码主轴世界角上 |

**三个局限**

1. 抓取点位置本身不可定义（只能整体平移）
2. `OffsetX/OffsetY` 是世界系常量，**不随产品角度旋转** → 补偿偏心夹爪时只在单一角度准确
3. 画面叠加没有单独的抓取点标记，现场无法直接核对

---

## 二、设计原则

> **在产品局部坐标系（OBB 局部系）里定义抓取点，而不是在世界坐标系上叠加常量。**

理由：夹爪偏心、抓取点在产品上的位置，本质都是**产品坐标系**里的固定量。
世界系常量只适合表达"相机/机器人标定的固定残差"——两者物理意义不同，必须分开。

数学上：

```
world = R(θ) · localOffset + worldConstOffset
        └── 新增：抓取点（随产品旋转）      └── 现有：标定残差（方向固定）
```

保留 `OffsetX/OffsetY` 语义不变（标定残差），新增抓取点偏移，两者叠加。现场可以分开标定，互不干扰。

---

## 三、数据模型

`Models/Recipe.cs` 新增：

```csharp
/// <summary>抓取点定义方式。</summary>
public enum GrabPointMode
{
    /// <summary>矩形几何中心（默认，行为与改造前完全一致）。</summary>
    Center = 0,
    /// <summary>在矩形局部系里按 mm 偏移。</summary>
    OffsetFromCenter = 1,
}

/// <summary>抓取点定义方式。</summary>
public GrabPointMode GrabPointMode { get; set; } = GrabPointMode.Center;

/// <summary>抓取点沿长轴的偏移（mm）。正值 = 朝向产品头部。</summary>
public float GrabOffsetLongMm { get; set; } = 0f;

/// <summary>抓取点沿短轴的偏移（mm）。正值 = 面朝头部方向的右手侧。</summary>
public float GrabOffsetShortMm { get; set; } = 0f;
```

**默认 `Center` + 偏移全 0 ⇒ 与现状逐位一致**，老配方文件反序列化后自动落在默认分支，零迁移成本。

### 关于"绝对 mm"还是"归一化比例"

| 方案 | 现场标定方式 | 跨尺寸自适应 | 实现成本 |
|---|---|---|---|
| **绝对 mm**（推荐为主） | 卡尺量偏心量直接填 | ❌ 换尺寸需重填 | 需新增尺度访问（见第五节） |
| 归一化比例 `[-0.5, 0.5]` | 在画面上数比例 | ✅ 自动适配 | 低（纯图像系计算，无需尺度） |

**建议先做绝对 mm**——一个配方对应一个产品型号，尺寸固定，而偏心量是夹爪的物理属性、有具体数值，填 mm 最直观。
若后续出现"同一配方覆盖多规格"的需求，再加比例模式作为第三种 `GrabPointMode`，两者可并存。

---

## 四、⚠️ 计算位置：必须移到头尾消歧之后

这是本次改动**最主要的结构性风险**。

现状顺序（`DetectionRecordService.cs`）：

```
:350-357  取矩形中心 → imageX/imageY          ← 当前在这里定死
:375      ImageToPhysical → 世界坐标
:380-381  += OffsetX/OffsetY
:605-620  头尾消歧 → modelAngle / headFlipped ← 朝向到这一步才知道
:621-622  AngleTracker.Resolve(encoder, X, Y, modelAngle)
```

抓取点沿长轴的偏移**必须知道哪头是头**才能确定符号，所以**必须挪到 `:605` 之后**。

否则：长轴是无向轴（`Angle` 已做 ±180° 二义消除），偏移会有 50% 概率落到反方向——即一半产品偏到尾部。这是最容易出错的地方。

### 边界情况：朝向不可信时怎么办

消歧链是 `二维码 → 模型翻转 → 特征池 → 无向回退角`。当**全部失败**（只剩 `fallbackAngle`）时，朝向没有可信依据。

建议策略（按现场可接受度选）：

| 策略 | 行为 | 适用 |
|---|---|---|
| **A（推荐）** | 长轴偏移**不生效**（退化为中心），短轴偏移仍生效；日志/画面提示"朝向不可信" | 保守，不会把产品抓反 |
| B | 按无向轴的正方向加偏移 | 若产线来料朝向恒定，可接受 |
| C | 直接过滤该产品（不发 VGT） | 最严，但对回退场景过于激进 |

推荐 A：**宁可抓在中心，也不要抓反**。

---

## 五、单位换算：mm → 图像像素

抓取点在**图像坐标系**里算（原因见第六节），而配置单位是 mm，需要换算。

`CoordinateTransformer` 内部有 `_scaleX` / `_scaleY`（**像素/mm**）但**未公开**（`CoordinateSystemMapping/CoordinateTransformer.cs:41-47`）。且该变换是**非正交、X/Y 非均匀**的（三点标定的两条轴不保证垂直、比例可不同），不能简单地用单一 mm/px 常数。

**推荐做法：不新增尺度接口，直接用变换自身求该方向的局部比例。**

```csharp
// 取中心与"沿 dir 走 1 像素"两点的世界距离，反推该方向上的 像素/mm。
// 自动吸收非正交、X/Y 非均匀比例与镜像，无需额外的尺度字段。
double PixelsPerMmAlong(double ux, double uy)
{
    var (w0x, w0y) = transformer.ImageToPhysical(imgX, imgY);
    var (w1x, w1y) = transformer.ImageToPhysical(imgX + ux, imgY + uy);
    var mm = Math.Sqrt((w1x - w0x) * (w1x - w0x) + (w1y - w0y) * (w1y - w0y));
    return mm > 1e-9 ? 1.0 / mm : 0.0;
}
```

若标定未初始化（`IsInitialized == false`，纯像素模式），`ImageToPhysical` 是恒等映射 → 该函数返回 1，等价于"1 单位 = 1 像素"，符合现有 `isResize == false` 分支的行为。

---

## 六、为什么在图像坐标系里算

```csharp
var theta = maskMinAreaRect.Angle * Math.PI / 180.0;
var luX = Math.Cos(theta);
var luY = Math.Sin(theta);
if (headFlipped == true) { luX = -luX; luY = -luY; }   // 翻到头部方向

var svX = -luY;    // 短轴 = 长轴逆时针 90°
var svY = luX;

var kLong  = PixelsPerMmAlong(luX, luY);
var kShort = PixelsPerMmAlong(svX, svY);

var grabImgX = imgX + GrabOffsetLongMm  * kLong  * luX + GrabOffsetShortMm * kShort * svX;
var grabImgY = imgY + GrabOffsetLongMm  * kLong  * luY + GrabOffsetShortMm * kShort * svY;

var (grabWorldX, grabWorldY) = transformer.ImageToPhysical(grabImgX, grabImgY);
grabWorldX += offsetX;   // 现有语义：世界系标定残差
grabWorldY += offsetY;
```

**关键好处：镜像与旋转由标定矩阵自然吸收。**
`CoordinateTransformer.IsMirrored` 表示图像系与机器人系存在反射。若在世界坐标里直接加常量的"左/右"偏移，镜像下必须手工翻符号（`IsMirrored` 正是为此类问题引入的）。而在图像系里算完再整体变换，镜像/旋转/非均匀尺度全部由既有变换处理，**不需要任何额外的符号判断**。

---

## 七、界面

### 7.1 配方配置（`Views/RecipeWindow.xaml`，放在现有「标定补偿」组内或新增一组）

| 控件 | 说明 |
|---|---|
| 抓取点方式 | 下拉：矩形中心 / 自定义偏移 |
| 长轴偏移 (mm) | 正 = 朝头部，范围建议 ±999.99 |
| 短轴偏移 (mm) | 正 = 头部方向右手侧 |

建议与现有「标定补偿」**分组显示**并在 Tooltip 里写明区别：

- **抓取点偏移**：产品坐标系，随产品旋转 → 用于夹爪偏心、抓取点不在中心
- **平移补偿 OffsetX/Y**：世界坐标系，方向固定 → 用于安装/标定残差

这两组语义不同，混在一起现场必然搞错，务必在标题上体现。

### 7.2 画面叠加（`ViewModels/HomeViewModel.Draw.cs`，必做）

现在画了旋转矩形、长轴箭头、短轴分割线，但抓取点只是箭头与分割线的交点，不明显。

**必须新增一个醒目的抓取点标记**（例如十字 + 空心圆，颜色区分于现有的红/绿/蓝），并在偏移非零时用虚线连到矩形中心、标注当前偏移量。

理由：没有画面反馈，现场只能靠"发过去抓一下看结果"试错，标定效率极低。

### 7.3 示教方式

| 方式 | 成本 | 建议 |
|---|---|---|
| 填数值 + 画面看点位 | 低 | **先做这个** |
| 画面上点击/拖拽取点，自动反算偏移 | 中 | 二期（需逆变换 + 交互） |
| 用实拍图 + 期望点坐标反算 | 中 | 二期 |

一期"填数值 + 画面标记"就足够现场把偏心量标出来。

---

## 八、兼容性与回归测试

### 8.1 零行为变化的保证

- `GrabPointMode` 默认 `Center`，两个偏移默认 `0`
- 老配方 JSON 无这些字段 → 反序列化落默认值
- `Center` 分支不进入新逻辑，`WorldX/WorldY` 与改造前逐位相同

### 8.2 必测用例

| 用例 | 期望 |
|---|---|
| 默认配置（Center） | 抓取点 == 矩形中心，与改造前完全一致 |
| 长轴偏移 +10mm，头尾未翻转 | 沿长轴正方向偏移 10mm |
| 长轴偏移 +10mm，`headFlipped == true` | 沿长轴**反**方向偏移 10mm（关键回归点） |
| 短轴偏移，θ 取 0/90/180/270 四个象限 | 偏移方向随 θ 正确旋转 |
| 镜像标定（`IsMirrored == true`） | 结果与图像系几何一致，无需额外符号处理 |
| 朝向不可信（仅 `fallbackAngle`） | 按策略 A：长轴偏移不生效，短轴仍生效，有提示 |
| 标定未初始化（`IsInitialized == false`） | 1 单位 = 1 像素，不抛异常、不全零 |
| 抓取点超出矩形范围 | 不拦截（属合法配置），但画面/日志给提示 |

---

## 九、建议分期

| 阶段 | 内容 | 风险 |
|---|---|---|
| **一期** | `GrabPointMode.Center / OffsetFromCenter` + 长/短轴 mm 偏移 + 画面标记 + 单元测试 | 中（含编码顺序调整，见第四节） |
| 二期 | 比例模式（归一化，跨尺寸自适应） | 低 |
| 三期 | 画面上拖拽示教 | 中 |

一期就覆盖了"偏心夹爪"和"抓取点不在中心"两个主要诉求。

---

## 十、需要你确认的点

1. **实际需求是哪一类**？
   - (a) 夹爪偏心（有具体 mm 量值）→ 长/短轴 mm 偏移正好
   - (b) 抓取点按比例定位（如"靠头部 1/3 处"）→ 建议一期就加比例模式
   - (c) 只是整体挪（安装残差）→ 现有 `OffsetX/Y` 已够，无需本次改造

2. **"头部"定义是否稳定可用**？抓取点的长轴偏移方向锚定在头部（二维码 → 模型 → 特征池消歧）。
   若现场有大量产品**二维码读不到、也没启用角度模型**，朝向就只能靠特征池或回退角 ——
   这种情况下一期是否仍要求长轴偏移生效？还是按推荐策略 A 退化为中心？

3. **短轴正方向**（"头部方向右手侧"）是否与现场操作习惯一致？需要的话可以改为"左/右"下拉，内部换算。

4. **是否需要保留"世界系标定残差"与"抓取点偏移"两套**？我倾向保留（语义不同），
   但如果现场觉得容易混淆，也可以只保留抓取点偏移、把现有 `OffsetX/Y` 迁移过去（需要处理老配方的数值迁移）。

---

## 十一、实施记录（2026-09-15 当日完成）

用户决定：**按本方案实施，并移除"往世界坐标叠加常量"这套做法**（即第 10 节第 4 点选"只保留抓取点偏移"）。

### 相对本方案的调整

| 项 | 方案 | 实际实施 | 原因 |
|---|---|---|---|
| `GrabPointMode` 枚举 | 有（Center / OffsetFromCenter） | **去掉** | 0/0 天然即"中心"，多一个模式就多一个"值填了却被模式忽略"的坑 |
| `OffsetX`/`OffsetY` | 建议保留 | **降级为"仅用于迁移"的废弃字段** | 按用户要求移除世界系叠加；但字段必须保留才能读取旧配方做一次性迁移 |
| 旧值迁移 | 未在方案中单列 | **新增 `MigrateLegacyOffsets()`** | 不迁移的话，现场已填过补偿的配方会突然失去补偿、坐标整体偏移，属产线事故 |
| 计算入口 | 按角度 | **按方向向量** | 矩形在推理图坐标系算出、而中心已按缩放比还原到原图坐标系；非等比缩放会让角度失真 |
| 抓取点画面标记 | 方案列为"必做" | 已实现（洋红十字+方框，偏移非零时连线回中心） | 现场标定必须能看到点位 |

### 实际改动文件

| 文件 | 改动 |
|---|---|
| `MainAPP/Application/GrabPointCalculator.cs` | 🆕 纯计算核心（偏移向量 + 图像点 + 标定尺度反推） |
| `MainAPP/Models/Recipe.cs` | 新增抓取点偏移与迁移标志；`OffsetX/Y` 降级为迁移专用；新增 `MigrateLegacyOffsets()` 并在 `LoadFromFile` 调用 |
| `MainAPP/Application/DetectionRecordService.cs` | 删除 `offsetX/offsetY` 参数与世界系叠加；抓取点计算移到头尾消歧之后；`WorldX/WorldY` 改为抓取点；朝向不可信时长轴偏移退化 + 帧日志提示 |
| `MainAPP/ViewModels/HomeViewModel.Draw.cs` | 新增抓取点画面标记 |
| `MainAPP/Views/RecipeWindow.xaml` | 平移补偿 → 独立的「抓取点（产品局部坐标系，随角度旋转）」分组 |
| `MainAPP/ViewModels/RecipeViewModel.cs` / `.Inference.cs` | 属性改名；配方页预览同步改算抓取点，维持"所见即所发" |
| `MainAPP/Models/DbModel.cs` | `WorldX/WorldY` 语义注释更新为"抓取点（发送坐标）"；`ImageX/ImageY` 保持"产品中心"（二维码距离分析依赖它） |
| `MainAPP/ViewModels/HomeViewModel.Process.cs`、`MainAPP/Services/AI/AiChatService.cs` | 调用与上下文同步 |
| `MainAPP.Tests/Application/GrabPointCalculatorTests.cs` | 🆕 15 个测试 |
| `MainAPP.Tests/Models/RecipeTests.cs`、`Integration/RecipeFileIntegrationTests.cs` | 断言调整 + 迁移测试 |

### 验证结果

| 验证项 | 结果 |
|---|---|
| 编译 | ✅ 0 警告 0 错误 |
| 完整测试套件 | ✅ **706 通过 / 2 失败**（仍仅既有 `ProtocolTemplateRendererTests` 的取整失败） |
| 新增抓取点测试 | ✅ 15/15 通过（0 偏移原样返回中心、头尾翻转符号、四象限旋转、退化方向、未标定/已标定尺度、mm→px 端到端） |
| 迁移测试 | ✅ 通过（含"加载含旧 OffsetX/OffsetY 的文件应自动迁移"） |
| 程序启动 | ✅ 正常，无异常 |

### ⚠️ 现场必须核对的两点

1. **短轴"右为正"的方向取决于相机安装朝向** —— 代码无法自行判断。
   现场按画面上的洋红标记核对；若方向相反，**填负值**即可，不用改程序。
2. **迁移是近似的** —— 世界系常量与产品局部系偏移只在"产品角度恒定"时严格等价。
   迁移会打 Warning 日志（`[配方迁移]`）；若现场产品角度变化较大，迁移后需按实际抓取效果复核数值。

### 尚未做（方案中的后续阶段）

| 阶段 | 内容 | 说明 |
|---|---|---|
| 二期 | 归一化比例模式（相对矩形半长/半宽） | 适用于"同一配方覆盖多规格产品" |
| 三期 | 画面上拖拽示教抓取点 | 一期"填数值 + 看标记"已够现场标定 |

### 默认行为不变性

`GrabOffsetLongMm` / `GrabOffsetShortMm` 默认均为 0，且 `ComputeImageOffset` 在 0 偏移时**短路返回 (0,0)** ——
抓取点逐位等于矩形中心，与改造前行为完全一致，现有产线不受影响。
