<UserControl
    x:Class="MainAPP.Views.AiChatView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf">

    <!--
        AI 对话面板（A 部分：浏览器级体验）。
        UI 全部由 Deep Chat（MIT，Web Component）负责，本控件只是把 WebView2 指到
        AiWebHost 的本地端点（http://127.0.0.1:5190/）。同一份页面手机也能访问。
        底色与 Deep Chat 页面的深色主题一致，避免初始化期间闪白。
    -->
    <Grid Background="#141414">
        <wv2:WebView2
            x:Name="ChatWebView"
            DefaultBackgroundColor="#141414"
            Source="http://127.0.0.1:5190/" />

        <!-- WebView2 Runtime 缺失时的兜底提示（产线机可能未装运行时） -->
        <TextBlock
            x:Name="FallbackText"
            Margin="24"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Foreground="#E24B4A"
            TextAlignment="Center"
            TextWrapping="Wrap"
            Visibility="Collapsed" />
    </Grid>
</UserControl>
